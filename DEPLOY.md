# SSH Деплой для WordPress темы Sculptura

Эта система позволяет автоматически загружать тему WordPress и ассеты на сервер через SSH.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка конфигурации

Скопируйте пример конфигурации и заполните своими данными:

```bash
# Windows PowerShell
Copy-Item deploy.config.example.js deploy.config.js

# Linux/Mac
cp deploy.config.example.js deploy.config.js
```

Откройте `deploy.config.js` и заполните:

```javascript
ssh: {
  host: 'your-server.com',        // IP или домен вашего сервера
  port: 22,                        // Порт SSH (обычно 22)
  username: 'your-username',       // Ваш логин на сервере
  // password: 'your-password',   // Или используйте SSH ключ (рекомендуется)
}
```

### 3. Настройка SSH ключа (рекомендуется)

#### Вариант A: Использование существующего SSH ключа

Если у вас уже есть SSH ключ в `~/.ssh/id_rsa`, скрипт автоматически его использует. Просто убедитесь, что публичный ключ добавлен на сервер:

```bash
ssh-copy-id username@your-server.com
```

#### Вариант B: Создание нового SSH ключа

```bash
# Генерируем новый ключ
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# Копируем публичный ключ на сервер
ssh-copy-id username@your-server.com
```

#### Вариант C: Использование ключа из проекта

Если хотите использовать ключ из проекта, добавьте в `deploy.config.js`:

```javascript
privateKey: require('fs').readFileSync(require('path').join(__dirname, '.ssh/id_rsa')),
passphrase: 'your-passphrase-if-needed', // Если ключ защищён паролем
```

⚠️ **Внимание:** Не коммитьте приватные ключи в Git! Добавьте `.ssh/` в `.gitignore`.

### 4. Настройка путей на сервере

Укажите путь к директории темы WordPress на сервере:

```javascript
remote: {
  themePath: '/var/www/html/wp-content/themes/sculptura/',
  // Или относительный путь:
  // themePath: '~/public_html/wp-content/themes/sculptura/',
}
```

## 📦 Использование

### Деплой всего (тема + ассеты)

```bash
npm run deploy
```

### Деплой только темы WordPress

```bash
npm run deploy:theme
```

### Деплой только ассетов (CSS/JS/изображения)

```bash
npm run deploy:assets
```

## 🔧 Настройка путей

В файле `deploy.config.js` можно настроить:

```javascript
local: {
  themeSource: './wp-theme/',    // Путь к папке темы локально
  assetsPath: './build/',         // Путь к собранным ассетам
}
```

## 📝 Что деплоится?

- **Тема (`theme`)**: Все файлы из папки `wp-theme/`
- **Ассеты (`assets`)**: CSS, JS и изображения из папки `build/` (загружаются в `assets/` на сервере)

## ⚠️ Важные замечания

1. **Файл `deploy.config.js` уже добавлен в `.gitignore`** и не будет закоммичен в Git
2. **Не храните пароли и приватные ключи в Git**
3. Перед деплоем ассетов убедитесь, что проект собран: `npm run dev` или `gulp dev`
4. Скрипт автоматически создаёт удалённые директории, если их нет

## 🐛 Решение проблем

### Ошибка подключения

- Проверьте правильность хоста, порта и имени пользователя
- Убедитесь, что SSH ключ добавлен на сервер: `ssh-copy-id username@host`
- Проверьте доступность сервера: `ping your-server.com`

### Ошибка прав доступа

- Убедитесь, что пользователь имеет права на запись в целевую директорию
- Проверьте права на директорию: `ls -la /path/to/theme`

### Файл deploy.config.js не найден

- Убедитесь, что файл создан: `cp deploy.config.example.js deploy.config.js`
- Проверьте, что файл находится в корне проекта

## 📚 Дополнительная информация

- SSH ключи: https://www.ssh.com/academy/ssh/key
- WordPress структура: https://wordpress.org/support/article/folder-structure/

