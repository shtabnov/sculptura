# 🔒 Исправление утечки Telegram Bot Token

## ⚠️ ВАЖНО: Токен был скомпрометирован

Токен бота был обнаружен в публичном репозитории GitHub. **Необходимо отозвать старый токен и создать новый.**

## Что было исправлено

1. ✅ Удалён токен из всех файлов в репозитории
2. ✅ Настроено безопасное хранение через переменные окружения или локальный файл
3. ✅ Добавлен `.telegram-config.js` в `.gitignore`
4. ✅ Создан пример файла `.telegram-config.example.js`

## Что нужно сделать СРОЧНО

### 1. Отозвать старый токен

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/revoke` или `/deletebot`
3. Выберите бота `@Second5155bot`
4. Подтвердите удаление/отзыв токена

### 2. Создать новый токен

1. Отправьте команду `/newbot` боту [@BotFather](https://t.me/BotFather)
2. Следуйте инструкциям (можно использовать то же имя бота)
3. Скопируйте новый токен

### 3. Настроить безопасное хранение токена

**Вариант А: Использование локального файла (рекомендуется для разработки)**

1. Скопируйте пример файла:
   ```bash
   cp .telegram-config.example.js .telegram-config.js
   ```

2. Откройте `.telegram-config.js` и заполните реальными значениями:
   ```javascript
   module.exports = {
     TELEGRAM_BOT_TOKEN: 'ваш_новый_токен',
     TELEGRAM_CHAT_IDS: ['242846482', '973736141'],
   };
   ```

3. Файл `.telegram-config.js` уже в `.gitignore` и не будет закоммичен

**Вариант Б: Использование переменных окружения (рекомендуется для продакшена)**

Установите переменные окружения перед запуском скрипта:

```bash
# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN = "ваш_новый_токен"
$env:TELEGRAM_CHAT_IDS = "242846482,973736141"

# Linux/Mac
export TELEGRAM_BOT_TOKEN="ваш_новый_токен"
export TELEGRAM_CHAT_IDS="242846482,973736141"
```

### 4. Обновить токен на сервере

После создания нового токена обновите его в `wp-config.php` на сервере:

```bash
node scripts/setup-telegram.js
```

Или вручную отредактируйте `wp-config.php` на сервере и замените старый токен на новый.

## Проверка

После настройки проверьте работу:

1. Заполните форму записи на сайте
2. Проверьте Telegram - должно прийти сообщение
3. Если не работает, проверьте логи WordPress

## Дополнительные меры безопасности

1. ✅ Никогда не коммитьте токены в Git
2. ✅ Используйте переменные окружения или локальные файлы конфигурации
3. ✅ Регулярно проверяйте репозиторий на утечки с помощью GitGuardian
4. ✅ Используйте GitHub Secrets для CI/CD

## История изменений

- **2025-11-06**: Удалён скомпрометированный токен из всех файлов
- **2025-11-06**: Внедрена система безопасного хранения токенов

